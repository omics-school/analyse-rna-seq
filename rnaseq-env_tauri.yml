name: rnaseq-env
# channels priority
# see https://bioconda.github.io/
# conda-forge has highest prioriy
channels:
    - defaults
    - bioconda
    - conda-forge
dependencies:
    - fastqc=0.11.6
    - bowtie2=2.3.4.3
    - htseq=0.9.1
    - samtools=1.8
    - conda-forge::ncurses=5.9
